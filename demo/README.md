# DataPulse Demo Files

These files are fictional and safe for local demos. They intentionally contain common tabular data problems:

- leading/trailing whitespace
- missing values
- duplicate rows
- messy headers
- empty columns
- CSV and TSV delimiter examples

Use `messy_sales.csv` for the primary demo flow and `messy_students.tsv` for the restored-rules or template workflow.
